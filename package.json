{
  "name": "smart-clothing-store",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix clothing-store-locator run build",
    "start": "npm --prefix clothing-store-locator start",
    "test": "npm --prefix clothing-store-locator test"
  }
}
